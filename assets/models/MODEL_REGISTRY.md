# Registro de modelos

Este registro sirve para documentar la procedencia y el estado de cada modelo exportado. Añade una fila cada vez que se genere una nueva versión.

| Fecha | Nombre | Formato | Ubicación | Hash SHA256 | Notas |
| --- | --- | --- | --- | --- | --- |
| 2024-05-01 | best_float16 | `.tflite` | `assets/models/best_float16.tflite` | _pendiente_ | Exportación base usada para validación inicial. |

> **Nota:** Si almacenas los modelos en un servicio externo (S3, GCS, Release de GitHub, etc.), reemplaza la ruta local por la URL permanente y conserva una copia local mínima para pruebas.
